# {{ ansible_managed }}

{{ haproxy_logs_directory }}/haproxy*.log {
    missingok
    notifempty
    sharedscripts
    rotate {{ haproxy_logrotate_retention }}
    daily
    compress
    postrotate
        reload rsyslog >/dev/null 2>&1 || true
    endscript
}

